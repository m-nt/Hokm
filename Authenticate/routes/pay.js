const express = require("express");
const router = express.Router();
const merchant_id = require("../../conf.json")
const Pay = require("../models/Payments")

router.post("/init", (req, res) => {
    const {}
});
